To run the streamlit folder follow below instructions.

inside the streamlit folder commands are:-
uvicorn server:app --reload (uvicorn will run from this command)

inside the static folder commands are:-  (the file stucture will be 'static\streamlit\app.py')
streamlit run streamlit/app.py
